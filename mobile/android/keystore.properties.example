# Copy to keystore.properties (gitignored) or set env vars for CI.
# Never commit real credentials or the keystore file.
storeFile=release.keystore
storePassword=your-store-password
keyAlias=upload
keyPassword=your-key-password
